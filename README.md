# 🚨 Incident Management API

Микросервис для управления инцидентами. Позволяет операторам и системам отслеживать проблемы с самокатами, точками выдачи и отчетами, предотвращая потерю информации в чатах.

---

## ✨ Функциональность

- 🆕 **Создание инцидентов** - Регистрация новых проблем от операторов и систем
- 📋 **Просмотр инцидентов** - Получение списка с фильтрацией по статусу
- 🔄 **Обновление статусов** - Изменение статуса обработки инцидентов  
- 🗑️ **Удаление инцидентов** - Удаление завершенных или ошибочных записей
- 🔍 **Поиск и фильтрация** - Гибкий поиск по различным параметрам

---

## 🛠 Технологический стек

| Технология | Назначение |
|------------|------------|
| **Python 3.11+** | Основной язык программирования |
| **FastAPI** | Современный асинхронный веб-фреймворк |
| **SQLAlchemy 2.0** | ORM с асинхронной поддержкой |
| **PostgreSQL** | Надежная реляционная база данных |
| **Alembic** | Система миграций базы данных |
| **Poetry** | Управление зависимостями и виртуальным окружением |
| **Pydantic** | Валидация данных и сериализация |

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/UCarTopDoerTask.git
cd UCarTopDoerTask
```

### 2. Установка зависимостей
```bash
poetry install
```

### 3. Настройка окружения
Создайте файл `.env` в корне проекта:
```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=password
DB_NAME=incidents_db
ENV=DEV
```

### 4. Настройка базы данных
Убедитесь, что PostgreSQL запущен, и создайте базу данных:
```sql
CREATE DATABASE incidents_db;
```

### 5. Применение миграций
```bash
alembic upgrade head
```

### 6. Запуск приложения
```bash
poetry run uvicorn src.main:app --reload
```

**Приложение доступно по адресу:** http://localhost:8000

---

## 📡 API Endpoints

### 🎯 Инциденты

| Метод | Endpoint | Описание | Статусы |
|-------|----------|-----------|---------|
| `GET` | `/incidents/` | Получить все инциденты | `200 OK` |
| `GET` | `/incidents/?status={status}` | Фильтр по статусу | `200 OK` |
| `GET` | `/incidents/{id}` | Получить инцидент по ID | `200 OK`, `404 Not Found` |
| `POST` | `/incidents/` | Создать новый инцидент | `201 Created`, `422 Validation Error` |
| `PATCH` | `/incidents/{id}` | Обновить инцидент | `200 OK`, `404 Not Found` |
| `DELETE` | `/incidents/{id}` | Удалить инцидент | `200 OK`, `404 Not Found` |

### 📝 Примеры использования API

#### Создание инцидента
```bash
curl -X POST "http://localhost:8000/incidents/" \
  -H "Content-Type: application/json" \
  -d '{
    "description": "Самокат №123 не в сети более 2 часов",
    "source": "monitoring"
  }'
```

#### Получение инцидентов с фильтром
```bash
curl -X GET "http://localhost:8000/incidents/?status=new"
```

#### Обновление статуса инцидента
```bash
curl -X PATCH "http://localhost:8000/incidents/1" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "in_progress"
  }'
```

---

## 🗄 Модель данных

### 📊 Incident

| Поле | Тип | Обязательное | Описание |
|------|-----|---------------|-----------|
| `id` | `Integer` | ✅ | Уникальный идентификатор (автоинкремент) |
| `description` | `String` | ✅ | Подробное описание инцидента |
| `status` | `Enum` | ✅ | Статус обработки |
| `source` | `Enum` | ✅ | Источник поступления |
| `created_at` | `DateTime` | ✅ | Время создания (автоматически) |

### 📋 Статусы инцидентов
- 🆕 **new** - Новый инцидент
- 🔄 **in_progress** - В работе
- ✅ **resolved** - Решен
- 🔒 **closed** - Закрыт

### 📮 Источники инцидентов
- 👨‍💼 **operator** - Оператор
- 📊 **monitoring** - Система мониторинга  
- 🤝 **partner** - Партнер
- ⚙️ **system** - Система

---

## 🔧 Утилиты разработки

### Миграции базы данных
```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Описание изменений"

# Применить миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1

# Показать историю миграций
alembic history

# Проверить текущее состояние
alembic current
```

### Документация API
После запуска приложения доступна автоматическая документация:

- **Swagger UI** 📚: http://localhost:8000/docs
- **ReDoc** 📖: http://localhost:8000/redoc

---

## 📁 Структура проекта

```
UCarTopDoerTask/
├── 📁 src/                    # Исходный код приложения
│   ├── 📁 config/            # Конфигурация приложения
│   │   ├── settings.py       # Настройки приложения
│   │   └── dependencies.py   # Зависимости и подключение к БД
│   ├── 📁 models/            # Модели SQLAlchemy
│   │   ├── base.py          # Базовая модель
│   │   └── incident.py      # Модель инцидента
│   ├── 📁 schemas/           # Pydantic схемы
│   │   └── incident.py      # Схемы валидации инцидентов
│   ├── 📁 managers/          # Бизнес-логика
│   │   └── incident.py      # Менеджер инцидентов
│   ├── 📁 routers/           # API endpoints
│   │   └── incident.py      # Роутер инцидентов
│   └── main.py              # Точка входа приложения
├── 📁 alembic/              # Миграции базы данных
│   ├── versions/            # Файлы миграций
│   └── env.py               # Конфигурация Alembic
├── 📄 .env                  # Переменные окружения
├── 📄 pyproject.toml        # Конфигурация Poetry
├── 📄 alembic.ini           # Конфигурация Alembic
└── 📄 README.md             # Документация
```